{
  "name": "craft-influx",
  "private": true,
  "version": "1.0.0",
  "description": "Build pipeline for the Influx CP UI.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.0.0"
  }
}
